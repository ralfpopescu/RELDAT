TO RUN:

server:
'python reldat-server.py <port> <window>'
<port>: port numbe
<window>: window size

client:
'python reldat-client.py <host>:<port> <window>'
<host>: host ip address
<port>: port numbe
<window>: window size

THEN:
'transform <filename>'
 - converts a text file to uppercase

 'disconnect'
 -client disconnect
